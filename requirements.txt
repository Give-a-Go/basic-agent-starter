# Requirements for Math Tutor Agent

# Install with extensions for Tensorix (includes greeting feature):
# Run this command directly (not from requirements.txt):
# pip install "google-adk[extensions]"

# Or install base package:
google-adk